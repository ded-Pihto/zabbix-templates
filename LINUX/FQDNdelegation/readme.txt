This template for monitoring FQDNs delegation for domain name via whois utlility
Set macro {$DOMAINLIST} as "FQDN1,FQDN2,...,FQDN" and set macro for host that template used.
FQDNs will be monitoring from this host via internet.

for example {$DOMAINLIST}="microsoft.com,gmail.com"
